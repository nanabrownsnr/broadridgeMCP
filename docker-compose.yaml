services:
  filesystem-mcp:
    build:
      context: ./filesystem-mcp
    env_file:
      - ./filesystem-mcp/.env
    ports:
      - "8011:8000"

  vision-mcp:
    build:
      context: ./vision-mcp
    env_file:
      - ./vision-mcp/.env
    ports:
      - "8012:8000"
