services:
  filesystem-mcp:
    restart: unless-stopped
    build:
      context: ./filesystem-mcp
    env_file:
      - ./filesystem-mcp/.env
    ports:
      - "8011:8000"
      - "9000-9100:9000-9100"
    volumes:
      - /home/nbrown:/workspace
    environment:
      - WORKSPACE_ROOT=/workspace

  vision-mcp:
    restart: unless-stopped
    build:
      context: ./vision-mcp
    env_file:
      - ./vision-mcp/.env
    ports:
      - "8012:8000"

  text-classifier-mcp:
    restart: unless-stopped
    build:
      context: ./text-classifier-mcp
    env_file:
      - ./text-classifier-mcp/.env
    ports:
      - "8013:8000"
    volumes:
      - /home/nbrown:/workspace

  recruitee-mcp:
    restart: unless-stopped
    build:
      context: ./recruitee-mcp
    env_file:
      - ./recruitee-mcp/.env
    ports:
      - "8014:8000"

  candidate-intelligence-mcp:
    restart: unless-stopped
    build:
      context: ./candidate-intelligence-mcp
    env_file:
      - ./candidate-intelligence-mcp/.env
    ports:
      - "8015:8000"

  docusign-mcp:
    restart: unless-stopped
    build:
      context: ./docusign-mcp
    env_file:
      - ./docusign-mcp/.env
    ports:
      - "8016:8000"

  pandadoc-mcp:
    restart: unless-stopped
    build:
      context: ./pandadoc-mcp
    env_file:
      - ./pandadoc-mcp/.env
    ports:
      - "8017:8000"
